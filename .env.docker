# Environment variables for Docker development with Caddy proxy

# Web app configuration
NEXT_PUBLIC_SIGNALING_URL=https://localhost:9443/api/signaling

# Signaling server configuration  
PORT=4000
NODE_ENV=development
CLIENT_URL=https://localhost:9443
USE_HTTP2=true

# Caddy configuration
CADDY_ADMIN=0.0.0.0:2019